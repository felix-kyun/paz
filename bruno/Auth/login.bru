meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{Server}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "felix",
    "password": "felixkyun"
  }
}

settings {
  encodeUrl: true
}
